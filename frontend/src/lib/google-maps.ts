// Stable reference — must be defined outside components to avoid re-render warnings
// All map components that share id 'rh-google-maps' must use the same libraries array.
export const GOOGLE_MAPS_LIBRARIES: ('places')[] = ['places'];
